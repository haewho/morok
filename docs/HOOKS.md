# Точки интеграции с Telegram

База: `62b56a07ca7e30e39f7fd00a6728d6bbd716ca1c`. При переносе проверяйте символы и окружающую семантику, не только номера строк. Документ фиксирует фактические вмешательства текущей ветки; отсутствие конфликтов git не подтверждает их корректность.

| Файл / символ | Событие и порядок | Поток / инвариант | Проверка |
|---|---|---|---|
| `settings.gradle`, `build.gradle`, `gradle/morok-config.gradle` | Выбор основного APK-модуля и чтение локальной конфигурации до module evaluation | Gradle; нет зависимости от чужих API/Firebase/signing fixtures | Полный build, morokPreflight, отказ release без credentials |
| `TMessagesProj/build.gradle`, `gradle/morok-library.gradle` | Генерация собственных BuildConfig fields и res directory | Gradle; Java/JNI namespace сохранён, ABI arm64 | Java/C++ build, APK abi report |
| `TMessagesProj_App/build.gradle`, `gradle/morok-app.gradle` | Собственная подпись/package/account type и проверка release до pre-build | Gradle; debug key локальный, release key постоянный, без подмены identity | apksigner/aapt2 и release validation |
| `BuildVars` fields / `getSmsHash` / `isBetaApp` | Инициализация приложения | API/Google/SMS поля только из своей конфигурации; чужой APK updater/passkeys identity отключены | BuildConfig+APK audit; авторизация требует device test |
| `MorokSettingsActivity` → `MorokUpdateActivity` / `MorokUpdateManager` / shared `FileProvider` | Только после явных Check, Download и Install; trust keys/endpoints читаются из APK, verified temp APK получает узкий cache URI непосредственно перед installer intent | Worker → UI; RSA manifest/anti-rollback/expiry/target до download, exact size+SHA-256+package+version+same installed signer до rename; redirects/background checks/silent install отсутствуют, upstream updater остаётся OFF | Update signer/JVM/source tests, Java compile, unconfigured emulator smoke, two signed release versions/tamper/retention device matrix, [APP_UPDATES](APP_UPDATES.md) |
| `LoginActivity.PhoneView.onNextPressed` | До запроса телефона/отправки auth request | UI; отсутствие API credentials объясняется до авторизации | Emulator login screen; авторизованная регрессия отдельно |
| `LocaleController.getStringInternal`, `MorokBrand.localizedName` | До обращения к облачному language pack для own identity keys | UI/локализация; обновлённый language pack не заменяет MOROK на Telegram, прочие строки штатные | Locale/unit resource audit, emulator ru/en |
| `ContactsController.checkAppAccount` и остальные AccountManager queries | Создание/поиск Android accounts | Штатные потоки; тип аккаунта равен runtime package, не захватывает официальный клиент | XML/Java identity audit, соседняя установка/контакты |
| `res/xml/auth.xml`, `auth_menu.xml`, `sync_contacts.xml` | Android AccountManager UI и sync registration | Ресурс `morok_account_type` задаётся отдельно для debug/release | Слитый manifest/resources и device regression |
| `PushListenerController.GooglePushListenerServiceProvider.hasServices/onRequestPushToken` | До Firebase init/token work | Возврат при отсутствии собственного Firebase; не выдаёт FCM готовность | Debug without Firebase, позже собственный FCM/device test |
| Основной manifest и `config/*/AndroidManifest*.xml` | Установка, запуск, providers, receivers, backup | Раздельные authorities; Memory receiver/provider не экспортируются; cloud+device transfer закрытых данных исключены | Merged manifest, APK install, Keystore/instrumentation |
| `LauncherIconController.LauncherIcon`, `AppIconsSelectorCell.updateIconsVisibility` | Исправление/выбор launcher alias | UI; все старые alias ведут к Личине; предложен только утверждённый знак | Ресурсы и launcher screenshots |
| `res/values*/styles.xml` и brand assets | Splash и adaptive/monochrome icons | Android resource selection; исходный логотип не возвращается при смене маски | XML references, raster previews, emulator launch |
| `IntroActivity.createView` | До авторизации | UI; MOROK mark/title, доступ к настройкам и прокси без входа | Emulator onboarding/navigation |
| `ProfileActivity` MOROK row / settings search | Раздел настроек аккаунта | UI; обычная навигация не заменяется; экран переноса использует только системный SAF и собственные настройки; единая диагностика читает только локальные состояния и агрегированные счётчики | Native screen/search/transfer/diagnostics UI, settings and diagnostic report tests, [APPEARANCE](APPEARANCE.md), [SETTINGS_TRANSFER](SETTINGS_TRANSFER.md), [DIAGNOSTICS](DIAGNOSTICS.md) |
| `NotificationsController.getShortStringForMessage` / `getStringForMessage` → `MorokAppProfiles.showsNotificationContent`; `showNotifications` после apply | После passcode gate и до штатного выбора preview-текста; story push проверяется до возврата исходного payload; после записи локального флага существующие уведомления перестраиваются | Notification queue/caller; account-local override может только сузить штатный preview, возвращает fail-open при недоступном аккаунте, не меняет server notification settings и не скрывает возможное имя отправителя/чата | Settings app-profile JVM/source tests, Java compile, authenticated FCM/private/group/channel/story/second-account/lock-screen matrix, [APP_PROFILES](APP_PROFILES.md) |
| `VoIPHelper.startCall(User, …)` → `initiatePrivateCall` / `MorokCallConfirmation.request` | После frozen/private/connectivity и microphone/camera permission checks, непосредственно до существующего `initiateCall` | UI; OFF проходит штатно, ON допускает private audio/video call только по положительной кнопке; dismiss/error/repeated tap не звонит; incoming и group overload не изменены | Safety settings/source tests, Java compile, outgoing audio/video/permission/Cancel/double-tap/incoming/group device matrix, [SAFETY](SAFETY.md) |
| `ChatActivity` / `PeerStoriesView` `needStartRecordVideo` → `MorokRoundVideoConfirmation.guardedState` | UI, после штатного выбора record action и до `InstantCameraView.send`; только ветвь send/preview | OFF сохраняет `state=1`; ON заменяет только immediate send `1` на штатный preview `3`; preview send `4`, cancel, encoder/muxer/upload, schedule и round metadata не заменяются | Safety settings/source tests, Java compile, physical record/lock/preview/trim/cancel/send/schedule/story-reply/recipient matrix, [SAFETY](SAFETY.md) |
| `ChatActivity` header menu / `updateTitle` → `MorokChatMetadataActivity` / encrypted store | Пункт добавляется только обычному dialog; alias загружается async после создания/resume; текст подменяется после штатного `setTitle` и до parent title | UI + последовательная I/O очередь; secret/Saved/Replies/service modes исключены; upstream trust/status icons и subtitle сохраняются; editor/store не делают network/database calls | Chat metadata JVM/source tests, Java compile, authenticated private/group/channel/topic/logout matrix, [CHAT_METADATA](CHAT_METADATA.md) |
| `ChatActivity` header menu → `MorokReplyTemplatesActivity` / `expandMorokReplyTemplate` / `insertMorokReplyTemplate` | Выбор доступен только в обычном writable dialog; на UI-потоке подставляются текущее показанное имя/first name и локализованные дата/время, затем экран требует preview-confirm и только после него вызывает `replaceWithText(cursor, 0, …)` | UI; вычисленные значения не сохраняются, existing draft и выделенные символы не удаляются, message limit проверяется до mutation, send button/protocol не вызываются; secret/Saved/Replies/read-only/service modes исключены | Template variable/JVM/source tests, Java compile, authenticated private/group/channel/topic/alias/locale/draft matrix, [REPLY_TEMPLATES](REPLY_TEMPLATES.md) |
| `ChatActivity` header menu → `MorokSavedDraftActivity` / `restoreMorokSavedDraft` | Экран получает снимок текущего plain text; restore повторно проверяет message limit и при непустом composer требует подтверждение до `replaceWithText(0, length, …)` | UI; штатный Telegram draft/reply/topic остаётся отдельным, send/protocol не вызываются; secret/Saved/Replies/read-only/service modes исключены | Draft JVM/source tests, Java compile, authenticated restart/private/group/channel/topic/replace matrix, [SAVED_DRAFTS](SAVED_DRAFTS.md) |
| `TextSelectionHelper` floating toolbar → callback / `ChatActivity.insertMorokSelectedText` | MOROK item создаётся рядом со штатными Copy/Quote/Translate и виден только при `canCopy()` + обычном writable non-secret chat; выбранный текст вставляется в cursor с length check | UI; handles/selection остаются upstream, clipboard/send/storage/protocol не вызываются; peer/message `noforwards` скрывает действие | Selection source tests, Java compile, authenticated text/caption/emoji/RTL/noforwards matrix, [TEXT_SELECTION](TEXT_SELECTION.md) |
| `ChatActivity` message-cell delegate `hasDoubleTap` / `onDoubleTap` → `MorokInteractionGate.allowsDoubleTapReaction` | До штатной проверки выбранной/доступной реакции и повторно до `selectReaction` | UI; account-local OFF только отвергает chat-message double tap; default/read failure сохраняет Telegram behavior; long press, explicit reaction UI и media gestures не меняются | Interaction settings/source tests, Java compile, authenticated private/group/channel/forum/custom-reaction matrix, [INTERACTIONS](INTERACTIONS.md) |
| `LaunchActivity` `FlagSecureReason`, `BubbleActivity`/`ExternalActionActivity` onCreate, `AndroidUtilities.allowScreenCapture`, PhotoViewer/StoryViewer/PaymentForm/TranslateAlert secure branches | При создании окна, немедленно после локального переключения и перед каждым известным upstream clear/rebuild secure flags | UI/window/SurfaceView; MOROK protection может только добавить secure policy, штатные passcode/secret/noforwards/payment причины сохраняются; server state не меняется | Safety source test, Java compile, emulator FLAG_SECURE/screenshot/restart, physical recorder/recent/bubble/story/photo/payment matrix, [SAFETY](SAFETY.md) |
| `LiteMode.isEnabled`, blur3 drawable/provider/source classes | До затратного capture/render и при выборе цвета fallback | UI/render; отказ от прозрачного draw/capture на покрытых путях, без пересоздания чатов | Полная таблица символов и ограничений в [APPEARANCE](APPEARANCE.md) |
| `ChatActivity` `OPTION_MOROK_REMEMBER`, меню / `processSelectedOption` | Явное действие для одного поддерживаемого сообщения | UI; eligibility до снимка; файловая/crypto запись асинхронно; ни отправки, ни обхода TTL/noforwards | Memory domain tests + device acceptance |
| `UserConfig.clearConfig` → `MorokMemoryStore.onLogout` | До очистки и повторного использования слота | Синхронная revocation + фоновые удаления; данные и grants не переносятся в новую сессию | Identity tests, logout/key/file fault injection |
| `UserConfig.clearConfig` → `MorokChatMetadataStore.onLogout` | После Memory revocation и до очистки/reuse account slot | Синхронный durable marker по stable user ID; удаление отдельного Keystore key и noBackup каталога идёт в последовательной очереди | Chat metadata JVM/source tests; device logout/key-loss test |
| `UserConfig.clearConfig` → `MorokReplyTemplateStore.onLogout` | Во вложенном `finally` после вызовов Memory/chat metadata и до очистки/reuse account slot | Даже ошибка предыдущего private-store logout не пропускает durable template revocation; key/files удаляются последовательно | Template source tests; device logout/key-loss test |
| `UserConfig.clearConfig` → `MorokSavedDraftStore.onLogout` | В следующем вложенном `finally` до очистки/reuse account slot | Ошибка любой предыдущей private-store очистки не пропускает durable draft revocation; отдельные key/files удаляются последовательно | Draft source tests; device logout/key-loss test |
| `ApplicationLoader.postInitApplication` | После штатной загрузки аккаунтов | UI dispatch: proxy.start, reminder.restoreAll, MemoryObserver.start; idempotent observers | Restart/offline emulator; authenticated restart отдельно |
| `MessagesController.processUpdateArray` → `MemoryObserver.beforeTelegramStorage*` | После построения MessageObject для allowlisted new/edit либо сразу при delete/history update, до Telegram storage mutations | Update-processing thread делает bounded immutable snapshots и ждёт подтверждённый encrypted AtomicFile journal commit до 5 секунд; new пишутся одной пачкой update, отказ ставит capture-gap и не отменяет Telegram update; index/replay идут отдельно | Java compile, Memory domain/hook-order checks, authenticated crash/new/edit/delete device test |
| `MessagesController.getDifference/getChannelDifference` direct `new_messages` branches | После нормализации dialog/unread и построения MessageObject, до прямого `putMessages`; `channelDifferenceTooLong.messages` — до `overwriteChannel` | Stage/storage queues; покрывает catch-up payload, который не проходит через `processUpdateArray`; encrypted/secret исключаются общей eligibility; journaled new identity видна следующему edit/delete до async replay | 7 hook-order checks, Java compile, offline catch-up/fast edit-delete authenticated device test |
| `MemoryObserver.didReceivedNotification` | `didReceiveNewMessages`, `replaceMessagesObjects`, `messagesDeleted`, `historyCleared` после штатной обработки | Резервный путь пишет событие с тем же стабильным journal ID; new требуют account enable + chat allowlist, edit/delete применяются только к сохранённым ключам; replay идемпотентен | Memory/settings tests, [MEMORY](MEMORY.md), catch-up device test |
| `LocalHistoryImporter` → `MessagesStorage.getStorageQueue/getDatabase` | Только после явного выбора чата и подтверждения читает последние positive-ID строки `messages_v2` | Telegram storage queue; read-only SQLite, максимум 100 строк, без `loadMessages`, MTProto request и media download; те же secret/TTL/noforwards eligibility и stable-user check | Memory limit test, Java compile, authenticated cache-only device test |
| `MorokArchiveActivity` / `MorokMemoryStore.write/copyAttachment/cleanAutomaticArchive` | Account-local policy читается перед каждым commit автоматической проекции и перед копированием уже готового кешированного оригинала; уменьшение срока/бюджета требует UI-confirm и запускает явную очистку | Последовательная Memory queue; whitelist 30–365 дней, 64–256 МиБ, 1–8 МиБ и never/Wi-Fi/any; самые старые automatic cards удаляются первыми, manual cards не удаляются; сеть классифицируется только для локального copy, FileLoader/download не вызывается | Settings/Memory JVM tests, hook source checks, Java compile, authenticated Wi-Fi/mobile/cleanup device matrix, [MEMORY](MEMORY.md) |
| `MorokMemoryActivity` → `MorokMemoryStore.exportSelected` | Только после long-press многовыбора, plaintext-warning и результата системного `ACTION_CREATE_DOCUMENT` | Последовательная Memory queue пишет ZIP прямо в SAF `content://`; перед каждым blob проверяет активную незаблокированную сессию, AEAD/размер/SHA-256; внутренний TL/key не экспортируется, plaintext temp-файла нет | Memory export policy/source checks, Java compile, authenticated SAF/device test, [MEMORY](MEMORY.md) |
| `LaunchActivity.handleIntent` → OPEN_MEMORY | После обычной проверки passcode | UI; stable user ID разрешается в актуальный slot; неверный аккаунт не открывается | Reminder/source navigation/device lock test |
| `ProxyRotationController.didReceivedNotification`, `checkProxyAndSwitchRunnable`, `switchToAvailable` | Перед встроенными проверками и переключением | UI; флаг выключенной ротации действительно запрещает новые probes/переключения | Proxy review/tests и network failover device test |
| `MessagesController.sendTyping` → `MorokPrivacy.allowsTypingAction` | После штатной проверки peer/self и до `sendingTypings`/создания request | Поток вызывающего UI/worker path; при включённой account-policy возвращает `false` без локальной фиктивной отправки; chat exception разрешает путь, cancel разрешён; при ошибке настроек штатное поведение | Settings/privacy JVM tests, Java compile, second-account device matrix |
| `EmojiAnimationsOverlay.sendCurrentTaps` → `MorokPrivacy.allowsTypingAction` | До создания прямого emoji-interaction request | UI; подавление очищает только локальный batch taps; bot draft cleanup и group-call speaking action остаются штатными | Settings/privacy JVM tests, Java compile, second-account emoji observation |
| `MessagesController.updateTimerProc` → `MorokPrivacy.allowsOnlineStatus` | До выбора штатной online/offline status-ветки | Stage/update timer; запрет не создаёт `offline=false`, отмена незавершённого status request и `offline=true` остаются штатными; ошибка настроек разрешает обычное поведение | Settings/privacy JVM tests, Java compile, foreground/reconnect/second-account matrix |
| `MessagesController.markMessageContentAsRead` → `MorokPrivacy.allowsContentRead` | После локальных `markMessagesContentAsRead`/`messagesReadContent`, до создания `messages.readMessageContents` или `channels.readMessageContents` для входящего voice/round video | Поток запуска воспроизведения; локальный played-state сохраняется, подавленное событие не ставится в очередь; обычный read cursor, отдельный mention-action, secret/TTL paths и другие типы содержимого остаются штатными. Если voice/video само является mention, его общий server content-read тоже подавляется. Ошибка настроек разрешает обычное поведение | Settings/privacy JVM tests, Java compile, voice/round-video private/group/channel/second-account matrix |
| `MessagesController.markDialogAsRead` / `completeReadTask` → `MorokPrivacy.allowsReadReceipt` | После локальных `processPendingRead`, dialog counters и notification cleanup, до создания обычной read task; повторно перед отправкой ожидающей task | UI вызывает локальную обработку, stage queue хранит штатную задержку только для разрешённых requests; включение режима удаляет созревшую task без отправки, подавленные cursors не копятся; server updates другой сессии продолжают обрабатываться; secret chats разрешены; ошибка настроек сохраняет штатное поведение | Settings/privacy JVM tests, Java compile, restart/reconnect/private/group/channel/forum/second-account matrix |
| `markDialogAsReadInTelegram` / `markAllTopicsAsReadInTelegram` | Явное действие «Отметить прочитанным» из списка чатов, тем, notification action или Android Auto создаёт принудительную read task после той же локальной обработки | UI/receiver → stage queue; bypass ограничен явно названными действиями, автоматическое открытие/закрытие чата и ответ из popup/Wear его не используют | Java compile, explicit-action device test and second-account observation |
| `StoriesController.markStoryAsRead` / `AlbumStoriesList.markAsRead` → `MorokPrivacy.allowsStoryViewReceipt` | После обновления локального max-read/cache, до `stories.readStories` и `stories.incrementStoryViews` | UI; suppression не откатывает локальное просмотренное состояние и не создаёт очередь; reaction/reply остаются явными серверными действиями; chat exception разрешает request; ошибка настроек сохраняет штатное поведение | Settings/privacy JVM tests, Java compile, private/channel/album/second-account matrix |
| `SendMessagesHelper.performSendMessageRequest` success → `maybeMarkRepliedMessageAsRead` | После успешного server response фактической отправки, до независимой обработки результата в UI | Network callback переносит opt-in действие на UI; только immediate reply на входящий positive message в том же обычном dialog; forum topic сохраняет thread ID; failure/edit/scheduled/secret исключены; settings failure не раскрывает cursor | Settings/privacy JVM tests, Java compile, text/media/forum/failure/second-account matrix |
| `SendMessagesHelper.sendMessage(SendMessageParams)` → `MorokPrivacy.ghostSendDelaySeconds` | После paid-message confirmation, до создания локального сообщения и выбора text/media protocol branch | Для explicit opt-in и активного Ghost задаёт штатный server `scheduleDate` +60 секунд и повторно входит в метод; ненулевой scheduleDate не изменяется. Unsupported retry/secret/welcome/quick-reply/TTL/ephemeral/paid/story/suggestion/live-location/dice/mono-forum/forum-main останавливается до local/network send; chat exception возвращает немедленное штатное поведение; settings failure возвращает zero delay | Settings/privacy JVM tests, Java compile, queue/cancel/unsupported/album/forum/second-account device matrix |
| `ChatActivity` `OPTION_MOROK_PRIVACY_EXCEPTION` | Контекстное меню сообщения читает и атомарно меняет account-local набор исключений | UI; до 256 dialog IDs, stable user namespace; exception разрешает typing/read/content-read/story-view в выбранном обычном чате, online остаётся глобальным; собственный settings UI удаляет одну или все записи без изменения других политик | Settings persistence/bounds tests, Java compile, private/group/channel UI matrix |
| `InstantCameraView` Camera2 session creation / `morokRoundVideoCaptureSize` | До открытия initial/switch capture session выбирает желаемый квадрат только при включённом experimental profile | UI/camera thread; OFF возвращает исходный `roundVideoSize`; существующие lifecycle, front/rear selection и surfaces сохранены | Settings tests, Java compile, OnePlus/Samsung camera matrix |
| `InstantCameraView.VideoRecorder.startRecording` / `prepareEncoder` | После получения фактического preview и до первого encoded frame валидирует source/AVC surface codec, задаёт профиль либо откатывается к upstream; opt-in журнал получает только plan/encoder metadata | Encoder thread; один square H264 encode, AAC/muxer/progressive upload не заменены; geometry не меняется после старта; OFF сохраняет upstream parameters и metadata; диагностика bounded и не получает media/path/account | Camera diagnostic/settings tests, Java compile, ffprobe before/send/recipient matrix |
| `Camera2Session.updateCaptureRequest` / `setMorokRoundVideoTuning` | При создании recording request улучшенного кружочка запрашивает только объявленные FPS/AF/EIS режимы; opt-in журнал фиксирует запрос или синхронный fallback | Camera2 handler; самый узкий range содержит 30; неподдержанное остаётся template default; отклонённый enhanced request один раз повторяется в upstream режиме; hook не применяется к другим camera consumers; журнал не заявляет capture-result confirmation | Camera diagnostic tests, Java compile, local report, OnePlus/Samsung motion/low-light matrix |

Память ещё не является полным архивом всех history-вариантов: hooks и encrypted WAL охватывают live/processUpdateArray, прямые `difference.new_messages`, `channelDifference.new_messages/messages`, а также стандартные edit/delete/history update types. Явный импорт ограничен последними 100 строками, уже доступными локально. `differenceTooLong` без message payload не позволяет восстановить пропущенный текст или точные удаления; полная authenticated матрица не выполнена. Код `MessagesStorage`, входящие seq/pts/qts и общий MTProto callback не изменены. Подробные гарантии и оставшиеся crash/network проверки — в `MEMORY.md`, `PRIVACY.md`, `PROXY.md`, `APPEARANCE.md` и `TESTING.md`.
